name: persist_theme_example
description: Demonstrates how to use the persist_theme plugin.
publish_to: 'none'

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  persist_theme:
    path: ../

flutter:
  uses-material-design: true
