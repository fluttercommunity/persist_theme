name: persist_theme
description: A flutter plugin for persisting the theme data. Support for Dark Modes.
version: 1.6.1+1
author: Rody Davis Jr <rody.davis.jr@gmail.com>
homepage: https://github.com/flutter_community/persist_theme
maintainer: Rody Davis (@rodydavis)

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  localstorage: ^3.0.1+4
  provider: ^4.0.2
  flutter_material_color_picker: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
